# secrets — never commit
.env
.env.*
!.env.example

# Go
/agent/bin/
*.exe
*.test
*.out

# Node / Next.js
node_modules/
/web/.next/
/web/out/
npm-debug.log*
yarn-error.log*

# Vercel CLI linkage (contains project ids; not secret, but local-only)
.vercel/

# SDK build output
/sdk/js/dist/

# internal / not part of the submission (kept locally)
/CLAUDE.md
/DEMO.md
*.dc.html
/qa/
/demo/SCRIPT.md
/demo/last-proof.json

# Foundry
/contracts/out/
/contracts/cache/
/contracts/broadcast/
# stray Foundry artifacts if forge is ever run from repo root
/out/
/cache/

# misc
.DS_Store
*.log

# npm auth (env-referencing; no secret, local only)
/sdk/js/.npmrc
